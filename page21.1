from pydelicious import get_popular,get_userposts,get_urlposts

def initializeUserDict(tag,count=5):
    user_dict={}

    for pl in get_popular(tag=tag)[o:count]:
        for p2 in get_urlposts(pl['href']):
            user=p2['user']
            user_dict[user]={}
        return user_dict
